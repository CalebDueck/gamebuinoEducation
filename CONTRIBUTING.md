# Contributing

For class gallery work, each student creates a separate `.sprite.txt` file under `class_gallery/submissions/`. Do not edit another student's file. Run `python tools/sprite_validator.py your_file.sprite.txt` before committing.
